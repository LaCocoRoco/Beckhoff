> ScopeViewSerializable 							(Node)
Title = scopeName									(Element)
	> Operating										(Node)
	RecordTime = recordTime							(Element)
	> Charts 										(Node) FOR
		> ScopeChartSerializable					(Node)
		Name = chartName

			> Style
			BackgroundColorValue 	= chartColor
			BorderColorValue 		= borderColor
			BaseTime 				= displayTime

			> XAxis
				> Style
				ColorValue 		= timeLineColor
				Ticks 			= timeTickCount
				GridColorValue 	= gridLineColor
				GridLineWidth 	= lineWidth
				WIRD NICHT ÜBERNOMMEN! axisTickCount

			> YAxes
				> ScopeYAxisSerializable
				Name = axisName
					> Style
					Enabled 	= axisVisible
					ColorValue 	= axisColor
					LineWidth 	= lineWidth
					AutoScale 	= autoscale
					ShowName 	= axisNameVisible
					AxisMin 	= valueMin
					AxisMax 	= valueMax

					> Channels
						> ScopeChannelSerializable
						Name = channelName
						IdentHandle = 1 -> getChannelName(ConnectedChannelHandle#1)
							
							> Acquisition
							DataType 		= dataType
							IndexGroup 		= indexGroup
							IndexOffset 	= indexOffset
							TargetPort 		= amsPort
							SymbolName 		= symbolName
							AmsNetIdString 	= amsNetId
							IsSymbolBased 	= symbolBased

							> Style
							LineWidth 		= lineWidth
							ColorValue 		= lineColor
							MarkColorValue 	= plotColor
							MarkSize 		= plotSize
							Antialias 		= antialias
							Marks 			= plotVisible
							Visible 		= channelVisible

> TriggerModule
	> TriggerGroups							
		> ScopeTriggerGroupSerializable (loop)
		Title 				= triggerGroupName
		TriggerPrePercent 	= triggerOffset
		Enabled 			= enabled
			> TriggerSets
				> ScopeTriggerSingleSetSerializable (loop)
				CombineOption 	= combine
				ReleaseOption 	= release
				Threshold 		= threshold
				ConnectedChannelHandle = setChannelName(ConnectedChannelHandle#1)
				// oder loop get x